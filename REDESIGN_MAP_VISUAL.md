## 神遠征 — 地圖視覺全面重設計

### 問題
用戶在手機上（430px 寬，portrait）睇唔清地圖同角色。現在地圖：
- 背景同路線顏色太接近，對比度不足
- 角色三點太小，同背景融為一體
- 地圖整體太空，缺少視覺焦點
- 用戶截圖顯示只能隱約看到路線，角色幾乎不可見

### 目標
重新設計地圖視覺，令用戶一眼就睇到：
1. 隊伍在哪裡（最明顯的元素）
2. 路在哪裡可以行（清晰的行走路線）
3. 出口在哪裡（明顯的標記）
4. 遭遇區在哪裡（有顏色區分）
5. 當前是哪張地圖（主題色調不同）

### 設計方向 — 高對比、清晰可讀

**配色方案（參考暗黑破壞神 / 月影之塔）：**
- 地圖背景：使用漸層而非純色（深色但有層次）
- 路線：明亮的米色/暖白色線條（類似泥土地板），寬度至少 40px
- 路線邊框：深色（比背景更深），營造「溝渠」效果
- 隊伍：大而醒目，帶脈動光暈，每個成員有明確的白色圓形輪廓
- 出口：金色發光門框 + 箭頭，不能miss
- 遭遇區：紅色脈動邊界的半透明區域
- 裝飾物：更多更密（石柱、火把、骸骨、水窪），增加地圖豐富度
- 障礙物：深色實心塊，有明確邊框

### 具體改動

#### 1. `src/phaser/mapRenderer.ts` — 完全重寫渲染邏輯
- 背景：雙色漸層（左上亮 → 右下暗），加細微噪點/紋理效果
- 路線：40-50px 寬，米白色（#d4c8a0）填充，深色邊框（#1a1510）
- 路線上每隔一段畫石板紋理線
- 障礙物：實心深色塊 + 高光邊框
- 裝飾物：數量加倍，更大更明顯
- 出口：金色矩形門框 + 脈動光暈 + 箭頭向上
- 遭遇區：紅色半透明 + 虛線邊界 + 脈動動畫
- 互動點（寶箱/神壇）：更大 icon，加發光效果

#### 2. `src/phaser/DungeonScene.ts` — 角色重新設計
- 角色圓形加大到 radius 20
- 外圈白色描邊 6px
- 大光暈 radius 50，亮度 0.3
- 加一個小名字標籤在頭頂（「戰」「法」「牧」之類）
- 脈動效果更明顯（上下移動幅度加大）
- 在角色下方加一個小方向指示箭頭

#### 3. `src/phaser/mapData.ts` — 增加地圖內容
- 每張地圖的裝飾物數量加倍（至少 15-20 個）
- 障礙物增加（至少 8-10 個）
- 遭遇區加大（radius 至少 60-80px）
- 路線加寬（width 至少 40）
- 出口加大（radius 至少 50）

#### 4. `src/phaser/config.ts` — 調整 viewport
- 降低 VIEWPORT_SIZE 或調整 zoom 確保手機上清楚

### 約束
- 所有 UI 繁體中文
- 必須通過 `npx tsc --noEmit`
- 只改動 Phaser 相關文件（mapRenderer, DungeonScene, mapData, config, fogOfWar）
- 不要改動 React UI 文件

### 參考視覺風格
想像一個暗色地牢遊戲：
- 地板是灰褐色石板路（很明顯）
- 牆壁/障礙是深灰色實心塊
- 角色是鮮豔彩色大圓點（帶光暈）
- 出口是金色閃閃發光
- 整體雖然暗但有高對比度，不是一片模糊

### 驗證
```bash
cd /Users/kevinyuen/mlps && npx tsc --noEmit
kill $(lsof -ti:5173) 2>/dev/null; sleep 1; cd /Users/kevinyuen/mlps && npx vite --host 0.0.0.0 &
```
